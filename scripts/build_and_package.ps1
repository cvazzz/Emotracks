# Placeholder packaging script for Windows PowerShell
# Build Flutter web (when present) and copy to backend static dir
# flutter build web
# Copy-Item -Recurse -Force frontend\build\web backend\static
# TODO: add version tagging, Docker image build, and artifact packaging
